<?xml version="1.0" encoding="UTF-8"?>
<ViewCell 
    xmlns:local="clr-namespace:Plapp"
    xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:iconize="clr-namespace:Plugin.Iconize;assembly=Plugin.Iconize"
             x:Class="Plapp.DataSeriesCell">
  <ViewCell.View>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Label Text="{Binding Tag.Id}"/>
            <Label Text="{Binding Latest.Date, Converter={local:DateTimeToDateStringConverter}}" 
                   Grid.Row="1"/>
            <Label Text="{Binding Latest.Data}"
                   Grid.Row="1"
                   Grid.Column="1"
                   />
            <Label Text="{Binding Tag.Unit}" Grid.Column="1"/>
            <iconize:IconButton 
                Grid.Column="2"
                FontSize="Large"
                Text="{StaticResource PlusIcon}"/>
        </Grid>
  </ViewCell.View>
</ViewCell>